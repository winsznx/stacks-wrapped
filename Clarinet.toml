[project]
name = "stacks-wrapped"
description = "On-chain Wrapped card participation registry for Stacks"
authors = []
telemetry = false
cache_dir = "./.cache"

[contracts.wrapped-registry]
path = "contracts/wrapped-registry.clar"
clarity_version = 5
epoch = "latest"

[repl.analysis]
passes = ["check_checker"]
check_checker = { trusted_sender = false, trusted_caller = false, callee_filter = false }
